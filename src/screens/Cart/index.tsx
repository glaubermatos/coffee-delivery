import { CartHeader } from "@components/CartHeader";
import { Container } from "./styles";

export const Cart: React.FC = () => {
    return (
        <Container>
            <CartHeader />
        </Container>
    );
}